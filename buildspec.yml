version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing dependencies
      - npm install express

  build:
    commands:
      - echo "Build running"

  post_build:
    commands:
      - echo "Build complete"

artifacts:
  files:
    - '**/*'
